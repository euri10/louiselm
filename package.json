{
  "name": "louiselm-site",
  "private": true,
  "scripts": {
    "site:build": "./scripts/build-site",
    "site:test": "node ./scripts/test-demo-config.mjs && ./scripts/test-public-artifact.sh",
    "site:test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "mystmd": "1.10.1"
  }
}
