# Plugin changelog

No plugin release has been published. `0.0.0` denotes an unreleased checkout;
the old ACP `0.1.0` literal was a placeholder, not a release.

The first proposal is `plugin-v0.1.0`, using the exclusive history baseline
`c905fdafa60370d1a07e6853bba741cce9da9870`. Review that baseline and the initial
version before enabling automation. Later entries are maintained by Release Please.
